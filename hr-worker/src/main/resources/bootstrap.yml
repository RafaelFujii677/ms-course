spring:
  application:
    name: hr-worker
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
  profiles:
    active:
    - test
    
management:
  endpoints:
    web:
      exposure:
        include: 
        - "*"